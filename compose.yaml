services:
  postgres:
    image: 'postgres:latest'
    container_name: postgres_g
    environment:
      - 'POSTGRES_DB=ovnis-projeto'
      - 'POSTGRES_PASSWORD=ovnis-projeto'
      - 'POSTGRES_USER=ovnis-projeto'
    ports:
      - '5432:5432'
